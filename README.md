# Sudoku

## 🔎 Структура проекта

```bash
├───src
│   ├───field_io.cpp      # модуль для работы с вводом/выводом поля в терминал. 
│   ├───field_io.h        # заголовочный файл для модуля field_io.
│   ├───game_logic.cpp    # модуль для реализации игры в Судоку.
│   ├───game_logic.h      # заголовочный файл для модуля game_logic.
│   └───main.cpp          # точка входа в программу.
└───build.ps1             # скрипт для сборки проекта на Windows.
```

## 💻 Запуск
Запустить скрипт для сборки в текущем каталоге. Запуск исполняемого файл включен в скрипт сборки.  
Файл с полем должен называться `field.txt` и должен находиться в текущей директории.
Убедитесь, что в текущей директории создана папка build. Именно в неё и сохраняется скомпилированный исполняемый файл.
